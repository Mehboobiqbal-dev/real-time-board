import React from 'react';
import { BoardData } from '../types';

const TaskChart = ({ boardData }: { boardData: BoardData }) => {
  return (
    <div>
      {/* TaskChart visualization will go here */}
      <p>TaskChart component placeholder</p>
      {/* You can use boardData here */}
    </div>
  );
};

export default TaskChart; 